import { colors } from "./colors";
import { sizes } from "./sizes";
import { fontFamily,fontSize,lineHeight,fontWeights } from "./fonts";

export const theme = {
  colors,
  sizes,
  fontFamily,
  fontSize,
  lineHeight,
  fontWeights
};